# 기동 가이드 (Startup Guide)

## 1. 시스템 요구사항

### 1.1 Backend
| 항목 | 요구사항 | 현재 설치 경로 |
|------|----------|----------------|
| Java | JDK 17 이상 | `C:\claude code\jdk-17.0.13+11` |
| Maven | 3.6 이상 | `C:\claude code\apache-maven-3.9.6` |
| Database | H2 (개발), PostgreSQL/MySQL (운영) | - |

### 1.2 Frontend
| 항목 | 요구사항 |
|------|----------|
| Node.js | 18.x 이상 |
| npm | 9.x 이상 |

---

## 2. 프로젝트 구조

```
C:\claude code\resource-management-system\
├── backend/                 # Spring Boot 백엔드
│   ├── src/
│   │   └── main/
│   │       ├── java/       # Java 소스코드
│   │       └── resources/  # 설정 파일
│   ├── pom.xml             # Maven 설정
│   └── start-backend.bat   # Backend 기동 스크립트 (Windows)
│
├── frontend/               # React 프론트엔드
│   ├── src/               # React 소스코드
│   ├── public/            # 정적 파일
│   ├── package.json       # npm 설정
│   └── start-frontend.bat  # Frontend 기동 스크립트 (Windows)
│
└── docs/                   # 문서
```

---

## 3. Backend 기동

### 3.1 배치 파일로 기동 (권장 - Windows)

**start-backend.bat 파일 사용:**
```cmd
C:\claude code\resource-management-system\backend\start-backend.bat
```

또는 파일 탐색기에서 `start-backend.bat` 더블클릭

**start-backend.bat 내용:**
```batch
@echo off
set "JAVA_HOME=C:\claude code\jdk-17.0.13+11"
set "MAVEN_HOME=C:\claude code\apache-maven-3.9.6"
set "PATH=%JAVA_HOME%\bin;%MAVEN_HOME%\bin;%PATH%"
cd /d "C:\claude code\resource-management-system\backend"
call "%MAVEN_HOME%\bin\mvn.cmd" spring-boot:run
```

### 3.2 명령 프롬프트에서 직접 기동

```cmd
:: 환경변수 설정
set "JAVA_HOME=C:\claude code\jdk-17.0.13+11"
set "MAVEN_HOME=C:\claude code\apache-maven-3.9.6"
set "PATH=%JAVA_HOME%\bin;%MAVEN_HOME%\bin;%PATH%"

:: backend 디렉토리로 이동
cd /d "C:\claude code\resource-management-system\backend"

:: Maven으로 실행
mvn spring-boot:run
```

### 3.3 IDE에서 실행
1. IntelliJ IDEA / Eclipse에서 프로젝트 열기
2. `ResourceManagementApplication.java` 파일 찾기
   - 경로: `backend/src/main/java/com/selim/resourcemanagement/ResourceManagementApplication.java`
3. 우클릭 → Run 'ResourceManagementApplication'

### 3.4 JAR 파일로 실행

```cmd
:: 환경변수 설정
set "JAVA_HOME=C:\claude code\jdk-17.0.13+11"
set "MAVEN_HOME=C:\claude code\apache-maven-3.9.6"
set "PATH=%JAVA_HOME%\bin;%MAVEN_HOME%\bin;%PATH%"

:: 빌드
cd /d "C:\claude code\resource-management-system\backend"
mvn clean package -DskipTests

:: 실행
java -jar target/resource-management-0.0.1-SNAPSHOT.jar
```

### 3.5 프로필 지정 실행

```cmd
:: 개발 환경 (H2 Database)
java -jar target/resource-management-0.0.1-SNAPSHOT.jar --spring.profiles.active=dev

:: 운영 환경 (PostgreSQL/MySQL)
java -jar target/resource-management-0.0.1-SNAPSHOT.jar --spring.profiles.active=prod
```

### 3.7 Backend 설정 (application.properties)

```properties
# 서버 포트
server.port=8080

# JWT 설정
jwt.secret=your-secret-key-here
jwt.access-token-expiration=3600000      # 1시간
jwt.refresh-token-expiration=604800000   # 7일

# H2 Database (개발 환경)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# H2 Console (개발 환경)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA 설정
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
```

### 3.8 운영 환경 Database 설정 예시

**PostgreSQL:**
```properties
spring.datasource.url=jdbc:postgresql://localhost:5432/resource_management
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=your-password
spring.jpa.hibernate.ddl-auto=update
```

**MySQL:**
```properties
spring.datasource.url=jdbc:mysql://localhost:3306/resource_management?useSSL=false&serverTimezone=Asia/Seoul
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=your-password
spring.jpa.hibernate.ddl-auto=update
```

---

## 4. Frontend 기동

### 4.1 배치 파일로 기동 (권장 - Windows)

**start-frontend.bat 파일 사용:**
```cmd
C:\claude code\resource-management-system\frontend\start-frontend.bat
```

또는 파일 탐색기에서 `start-frontend.bat` 더블클릭

**start-frontend.bat 내용:**
```batch
@echo off
cd /d "C:\claude code\resource-management-system\frontend"
npm start
```

### 4.2 명령 프롬프트에서 직접 기동

```cmd
:: frontend 디렉토리로 이동
cd /d "C:\claude code\resource-management-system\frontend"

:: 의존성 설치 (최초 1회 또는 package.json 변경 시)
npm install

:: 개발 서버 실행
npm start
```

### 4.3 프로덕션 빌드

```cmd
cd /d "C:\claude code\resource-management-system\frontend"

:: 빌드
npm run build

:: build 폴더에 정적 파일 생성됨
```

### 4.4 Frontend 환경 설정 (.env)

```env
# API 서버 주소
REACT_APP_API_URL=http://localhost:8080/api

# 환경 구분
REACT_APP_ENV=development
```

**운영 환경 (.env.production):**
```env
REACT_APP_API_URL=https://your-domain.com/api
REACT_APP_ENV=production
```

---

## 5. 동시 기동 (개발 환경)

### 5.1 배치 파일로 간편 기동 (권장)

파일 탐색기에서 각각 더블클릭:
1. `C:\claude code\resource-management-system\backend\start-backend.bat`
2. `C:\claude code\resource-management-system\frontend\start-frontend.bat`

### 5.2 명령 프롬프트 2개 사용

**명령 프롬프트 1 (Backend):**
```cmd
"C:\claude code\resource-management-system\backend\start-backend.bat"
```

**명령 프롬프트 2 (Frontend):**
```cmd
"C:\claude code\resource-management-system\frontend\start-frontend.bat"
```

### 5.3 기동 순서
1. **Backend 먼저 기동** - API 서버가 준비되어야 함
2. `Started ResourceManagementApplication` 메시지 확인
3. **Frontend 기동** - Backend 기동 완료 후 실행
4. 브라우저에서 http://localhost:3000 자동 열림

### 5.4 접속 정보

| 서비스 | URL | 설명 |
|--------|-----|------|
| Frontend | http://localhost:3000 | React 개발 서버 |
| Backend API | http://localhost:8080/api | REST API |
| H2 Console | http://localhost:8080/h2-console | DB 콘솔 (개발용) |

---

## 6. 서비스 중지 및 재기동

### 6.1 Backend 중지

**방법 1: 실행 중인 명령 프롬프트에서**
```
Ctrl + C 입력 후 Y 또는 Enter
```

**방법 2: 포트로 프로세스 찾아서 종료**
```cmd
:: 8080 포트 사용 중인 프로세스 확인
netstat -ano | findstr :8080

:: 결과 예시: TCP    0.0.0.0:8080    0.0.0.0:0    LISTENING    12345
:: 마지막 숫자(12345)가 PID

:: 프로세스 종료
taskkill /PID 12345 /F
```

**방법 3: 작업 관리자에서**
1. `Ctrl + Shift + Esc` 로 작업 관리자 열기
2. 세부 정보 탭에서 `java.exe` 찾기
3. 우클릭 → 작업 끝내기

### 6.2 Frontend 중지

**실행 중인 명령 프롬프트에서:**
```
Ctrl + C 입력
```

**포트로 프로세스 찾아서 종료:**
```cmd
:: 3000 포트 사용 중인 프로세스 확인
netstat -ano | findstr :3000

:: 프로세스 종료
taskkill /PID <PID번호> /F
```

### 6.3 Backend 재기동

```cmd
:: 1. 기존 프로세스 종료 (Ctrl+C 또는 taskkill)

:: 2. 다시 기동
"C:\claude code\resource-management-system\backend\start-backend.bat"
```

### 6.4 Frontend 재기동

```cmd
:: 1. 기존 프로세스 종료 (Ctrl+C)

:: 2. 다시 기동
cd /d "C:\claude code\resource-management-system\frontend"
npm start
```

### 6.5 전체 시스템 재기동 절차

```cmd
:: === 1단계: 모든 서비스 중지 ===
:: Frontend 중지 (Ctrl+C)
:: Backend 중지 (Ctrl+C)

:: === 2단계: 포트 사용 확인 ===
netstat -ano | findstr ":8080 :3000"
:: 사용 중인 포트가 있으면 해당 PID 종료

:: === 3단계: Backend 재기동 ===
"C:\claude code\resource-management-system\backend\start-backend.bat"
:: "Started ResourceManagementApplication" 메시지 확인까지 대기

:: === 4단계: Frontend 재기동 (새 명령 프롬프트에서) ===
cd /d "C:\claude code\resource-management-system\frontend"
npm start
```

### 6.6 코드 변경 후 반영

**Backend (Java 코드 변경 시):**
- Spring Boot DevTools가 설정되어 있으면 자동 재시작
- 자동 재시작이 안 되면 수동 재기동 필요

**Frontend (React 코드 변경 시):**
- Hot Reload 지원으로 자동 반영
- 브라우저 새로고침 불필요 (자동 갱신)

---

## 7. 초기 데이터 설정

### 7.1 기본 관리자 계정

시스템 최초 기동 시 다음 계정이 자동 생성됩니다:

| 항목 | 값 |
|------|-----|
| 이메일 | admin@selim.kr |
| 비밀번호 | admin123! |
| 권한 | SYSTEM_ADMIN |

### 7.2 초기 부서 데이터

- 경영지원본부
- 개발본부
- 영업본부

### 7.3 초기 메뉴 데이터

시스템 메뉴가 자동으로 생성됩니다.

---

## 8. 포트 변경

### 8.1 Backend 포트 변경

`application.properties` 파일 수정:
```properties
server.port=8081
```

### 8.2 Frontend 포트 변경

`package.json` 수정 또는 환경변수 설정:
```cmd
:: Windows
set PORT=3001 && npm start
```

### 8.3 Frontend API 주소 변경

Backend 포트를 변경한 경우 `.env` 파일도 수정:
```env
REACT_APP_API_URL=http://localhost:8081/api
```

---

## 9. 트러블슈팅

### 9.1 Backend 기동 오류

**오류: Port 8080 already in use**
```cmd
:: 포트 사용 중인 프로세스 확인
netstat -ano | findstr :8080

:: 프로세스 종료 (PID는 위 명령 결과에서 확인)
taskkill /PID <PID> /F
```

**오류: Java version mismatch**
```cmd
:: Java 버전 확인
java -version

:: JAVA_HOME 설정 확인
echo %JAVA_HOME%

:: 올바른 JAVA_HOME 설정
set "JAVA_HOME=C:\claude code\jdk-17.0.13+11"
```

**오류: JAVA_HOME이 설정되지 않음**
```cmd
:: start-backend.bat 파일 사용 권장 (환경변수 자동 설정)
"C:\claude code\resource-management-system\backend\start-backend.bat"
```

### 9.2 Frontend 기동 오류

**오류: node_modules 관련 오류**
```cmd
:: frontend 디렉토리로 이동
cd /d "C:\claude code\resource-management-system\frontend"

:: node_modules 삭제 후 재설치
rmdir /s /q node_modules
del package-lock.json
npm install
```

**오류: Port 3000 already in use**
```cmd
:: 포트 사용 중인 프로세스 확인
netstat -ano | findstr :3000

:: 프로세스 종료
taskkill /PID <PID> /F
```

### 9.3 CORS 오류

Backend에서 CORS 설정이 필요합니다. `WebConfig.java`:
```java
@Configuration
public class WebConfig implements WebMvcConfigurer {
    @Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/api/**")
                .allowedOrigins("http://localhost:3000")
                .allowedMethods("GET", "POST", "PUT", "DELETE")
                .allowCredentials(true);
    }
}
```

---

## 10. 운영 배포

### 10.1 Backend 배포

```bash
# JAR 빌드
cd backend
mvn clean package -DskipTests

# 서버로 JAR 복사 후 실행
nohup java -jar resource-management-0.0.1-SNAPSHOT.jar \
  --spring.profiles.active=prod \
  > app.log 2>&1 &
```

### 10.2 Frontend 배포

```bash
# 프로덕션 빌드
cd frontend
npm run build

# build 폴더를 웹서버(Nginx, Apache)로 배포
```

**Nginx 설정 예시:**
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        root /var/www/resource-management/build;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

---

## 11. 기동 체크리스트

### 개발 환경

- [ ] JDK 17 이상 설치 확인
- [ ] Node.js 18 이상 설치 확인
- [ ] Backend 기동 (포트 8080)
- [ ] Frontend 기동 (포트 3000)
- [ ] http://localhost:3000 접속 확인
- [ ] 로그인 테스트 (admin@selim.kr / admin123!)

### 운영 환경

- [ ] Database 서버 구성 완료
- [ ] application.properties 운영 설정 완료
- [ ] .env.production 설정 완료
- [ ] SSL 인증서 설정
- [ ] 방화벽 포트 오픈
- [ ] 로그 설정 확인
- [ ] 백업 정책 수립
